{
    "name": "clearcut-monorepo",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "client",
        "server"
    ],
    "scripts": {
        "client-install": "cd client && npm install",
        "server-install": "cd server && npm install",
        "install-all": "npm run client-install && npm run server-install",
        "build": "cd client && npm install && npm run build"
    }
}